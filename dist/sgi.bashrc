# Insert this command in the user's .bashrc or the system file
# /etc/profile to set the environment for using IMOD
#
# It assumes that IMOD is located in /usr/local - if not, modify

[ -r /usr/local/IMOD/IMOD-sgi.sh ] && source /usr/local/IMOD/IMOD-sgi.sh
